// Щоб ти міг написати такий крутий проект, тобі треба впевнено володіти JS-логікою. Давай швиденько розправимось із тими 3 задачами («AI Filter Simulation»), про які я писав раніше:

// Рядок у масив: Маєш рядок фільмів через кому — зроби з нього масив об'єктів.

// myMap: Додай кожному фільму властивість aiScore: 0.

// Debounce: Зроби функцію пошуку, яка спрацьовує тільки тоді, коли ти перестав друкувати назву фільму.

const rawMovies = "Inception, Interstellar, The Dark Knight, Memento, Tenet";

function workWithString(stringMovies) {
  const transformToArr = stringMovies.split(",");
  const result = transformToArr.map((movie, index) => {
    return {
      id: index + 1,
      title: movie.trim(),
    };
  });

  return result;
}

const arrToWork = workWithString(rawMovies);

function addScoreParam(arr, callback) {
  const result = [];

  for (const elementOfArr of arr) {
    result.push(callback(elementOfArr));
  }

  return result;
}

function addCallback(obj) {
  return {
    ...obj,
    aiScore: 0,
  };
}

const newArr = addScoreParam(arrToWork, addCallback);
console.log(newArr);

// [
//   { id: 1, title: "Inception" },
//   { id: 2, title: "Interstellar" },
//   // і так далі...
// ];

// 2. Мій Map (Data Enrichment)
// Уяви, що наш "ШІ" має оцінити кожен фільм. Для початку ми просто ініціалізуємо поле оцінки.

// Вхідні дані (масив з попередньої задачі):

// JavaScript

// Очікуваний результат:
// Новий масив, де у кожного об'єкта з'явився ключ aiScore.

// JavaScript
// [
//   { id: 1, title: "Inception", aiScore: 0 },
//   { id: 2, title: "Interstellar", aiScore: 0 }
// ]

// 3. Debounce (Optimization)
// Це найцікавіше. Нам потрібно імітувати введення тексту користувачем у пошуковий рядок.

// Тестовий сценарій:
// Ми створимо функцію searchMovie(query), яка просто виводить у консоль: "Searching for: [query]". Твоє завдання — загорнути її в debounce, щоб при швидкому виклику вона спрацювала лише один раз.

// Код для тестування:

// Твоя функція debounce має бути тут

function debounce(func, delay) {
  let timer;

  return function (...args) {
    if (timer) {
      clearTimeout(timer);
    }

    timer = setTimeout(() => func(...args), delay);
  };
}

function searchMovie(query) {
  console.log(
    `🔎 Виконую пошук: ${query} (час: ${new Date().toLocaleTimeString()})`,
  );
}

const debouncedSearch = debounce(searchMovie, 1000); // затримка 1 секунда

// Імітація швидкого друку користувача:
debouncedSearch("I");
debouncedSearch("In");
debouncedSearch("Inc");
debouncedSearch("Inception");

// // Очікуваний результат у консолі через 1 секунду ПІСЛЯ останнього виклику:
// // "🔎 Виконую пошук: Inception (час: ...)"
// Підказка для реалізації

// Задача 1: Використовуй методи .split() та .map(). Не забудь про .trim(), щоб прибрати зайві пробіли навколо назв.

// Задача 2: Можеш написати свою функцію myMap(array, callback) або використати вбудований метод.

// Задача 3: Тут знадобляться setTimeout та clearTimeout.

// Бажаєш, щоб я перевірив твій варіант рішення, коли ти його напишеш?

// CODEWARS

// Умова:
// Напиши функцію, яка приймає масив чисел (цілих) та цільове число (target). Функція повинна знайти в масиві два різні елементи, які в сумі дають значення target. Результатом має бути масив із двох індексів цих елементів: [index1, index2].

// Важливі деталі:

// Може бути кілька правильних відповідей — підійде будь-яка.

// Вхідні дані завжди валідні: масив містить мінімум 2 числа, всі вони — числа, а target обов'язково є сумою двох різних елементів із цього масиву.

// Ти не можеш використовувати одне й те саме число за одним і тим самим індексом двічі (елементи мають бути різними за позицією).

// Приклади:

console.log(twoSum([1, 2, 3], 4)); // повертає [0, 2] (бо 1 + 3 = 4)
console.log(twoSum([3, 2, 4], 6)); // повертає [1, 2] (бо 2 + 4 = 6)

function twoSum(nums, target) {
  const previousValues = {}; // 1. Твій "блокнот" для записів {число: його_індекс}

  for (let i = 0; i < nums.length; i++) {
    // 2. Йдемо по черзі по списку гостей
    const currentNumber = nums[i]; // Число поточного гостя
    const neededNumber = target - currentNumber; // 3. Обчислюємо "ідеальну пару"

    // 4. ПЕРЕВІРКА: "Чи бачив я раніше в блокноті того, хто мені потрібен?"
    if (previousValues[neededNumber] !== undefined) {
      // Якщо ТАК — ми знайшли пару!
      // Беремо індекс того, хто вже в блокноті, і поточний індекс i.
      return [previousValues[neededNumber], i];
    }

    // 5. ЗАПИС: "Якщо пари немає, записую поточного гостя в блокнот"
    // Ключ — це число (номер на грудях), значення — його позиція (індекс).
    previousValues[currentNumber] = i;
  }
}
// Пояснення 3. :У тебе є ціль (target): сума має дорівнювати 10.
// Ти йдеш по масиву і бачиш число (currentNumber), наприклад, 7.

// Питання: Яке число тобі потрібно знайти в масиві, щоб у сумі з твоєю сімкою отримати 10?
// Відповідь: Звісно, це 3.

// Як ти отримав цю трійку?
// 10(це target)−7(це currentNumber)=3

// Ось що робить цей рядок:

// Він вираховує "недостачу". Коли ти стоїш на поточному числі, ти заздалегідь обчислюєш, яке саме число зробить тебе "щасливим" (допоможе виконати умову задачі).

// Давай пройдемось по масиву [2, 11, 8] з ціллю 10:

// Ти зустрів 2:

// neededNumber = 10 - 2 = 8.

// Ти кажеш коду: "Так, я зараз тримаю двійку. Якщо я десь раніше бачив вісімку — ми перемогли!".

// У блокноті вісімки немає. Ти записуєш: "Я бачив 2 за індексом 0".

// Ти зустрів 11:

// neededNumber = 10 - 11 = -1.

// Тобі потрібна -1. У блокноті її немає. Записуєш: "Бачив 11 за індексом 1".

// Ти зустрів 8:

// neededNumber = 10 - 8 = 2.

// Тобі потрібна 2.

// Ти зазираєш у блокнот і — ОПА! — там написано: "Двійка була на індексі 0".

//   Все, пазл склався! Ти береш індекс двійки(0) і свій поточний індекс(2).

//
// Приклад на цифрах: twoSum([3, 2, 4], 6)

// Крок 1 (i = 0, число = 3):

// Шукаємо: 6 - 3 = 3.

// Чи є 3 у нашому порожньому блокноті? Ні.

// Записуємо в блокнот: { 3: 0 }.

// Крок 2 (i = 1, число = 2):

// Шукаємо: 6 - 2 = 4.

// Чи є 4 у блокноті? Ні (там тільки трійка).

// Записуємо в блокнот: { 3: 0, 2: 1 }.

// Крок 3 (i = 2, число = 4):

// Шукаємо: 6 - 4 = 2.

// Чи є 2 у блокноті? ТАК! Ми записали її на попередньому кроці.

// Блокнот каже, що число 2 має індекс 1. Наш поточний індекс — 2.

// Результат: [1, 2].

// Чому це оптимально?

// У звичайному циклі в циклі ти міг би перевірити одну й ту саму пару кілька разів. А тут ти проходиш повз кожного гостя лише один раз. Об'єкт у JavaScript працює надзвичайно швидко: він миттєво каже, чи є в ньому потрібне значення.
